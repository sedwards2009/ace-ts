{
  "name": "ace_ts",
  "version": "1.0.0",
  "description": "Ace editor but using TypeScript",
  "scripts": {
    "tsc": "tsc",
    "build": "tsc && rollup src/index.js --o dist/bundle.js -f iife --name ace"
  },
  "dependencies": {},
  "devDependencies": {
    "rollup": "^0.59.1",
    "typescript": "2.8.3"
  }
}
