{
  "name": "@extraterm/ace-ts",
  "version": "0.18.0",
  "description": "Ace editor but using TypeScript, for use in Extraterm",
  "scripts": {
    "build": "node build_scripts/build.js",
    "clean": "rimraf dist",
    "package": "yarn run build && yarn pack",
    "prepublish": "yarn run clean && yarn run build",
    "publish-pkg": "yarn publish --access public"
  },
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^12.6.2",
    "rimraf": "^3.0.2",
    "rollup": "^0.59.1",
    "shelljs": "^0.8.2",
    "typescript": "3.7.4"
  },
  "files": [
    "package.json",
    "README.md",
    "dist"
  ]
}
